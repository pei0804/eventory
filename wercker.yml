box: golang
build:
  steps:
    - setup-go-workspace

    - script:
        name: setup
        code: cd ./server

    - script:
        name: setup
        code: make setup

    - script:
        name: test
        code: server/make test
