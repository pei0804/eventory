box: golang
build:
  steps:
    - setup-go-workspace

    - script:
        name: setup
        code: make setup

    - script:
        name: build
        code: make build

    - script:
        name: test
        code: go test -v $(glide novendor)
